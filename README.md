# Social Task

Social-media task marketplace web app.

## Run
npm install
npm start

Open http://localhost:3000

Configure SMTP for real email OTP. Do not commit .env or secrets to GitHub.
SQLite needs persistent storage in production.
